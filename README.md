Command-line utilities for Virbos

## virbos-splash

Prints a random splash phrase to stdout.

---

# Install

You can install them by:
 - installing from the Virbos pacman repo
 - building a package (run `makepkg -si`)
 - by running `make install`.
